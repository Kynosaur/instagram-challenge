<% @posts.each do |post| %>
  <div class="post">
    <img src="<%= post.picture.url %>">
    <p>
    <%= simple_format(post.caption) %>
    </p>
  </div>
<% end %>

<%= link_to "New post", new_post_path %>
